# Aromatheca — agent entry point

All rules live in the private docs submodule: read **`docs/AGENTS.md`** before doing anything, then `docs/ai/pipeline.md`.

If `docs/` is empty, run `git submodule update --init docs` (requires access to the private repo). This public repo holds only the landing page, the API reference on GitHub Pages, and a data-services `docker-compose.yml`. Do not put product or planning content here.
