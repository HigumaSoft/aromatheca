@docs/AGENTS.md
@docs/CLAUDE.md

This is the public root repo. Rules are in `docs/AGENTS.md` (imported above). If the import shows nothing, the `docs` submodule is not checked out — stop and tell the owner.
